#Lists
thislist = ["apple", "banana", "cherry"]
print("thislist = ", thislist)

#lists can have duplicates
#there are methods for ordering lists
#we can use len() function to see the lenght

print("len(thislists) = ", len(thislist))
#lists can be integers, booleans, strings or a mix of the three
print("\nlists can be integers, booleans, strings or a mix of the three")
print("\nWe can use indexing to access lists")
print("thislist[1] = ", thislist[1])
print("thislist[-1] = ", thislist[-1])
print("thislist[2:5] = ", thislist[2:5] )